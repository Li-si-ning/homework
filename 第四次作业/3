#include<stdio.h>
void swap(int *x,int *y)
{
  int t=*x;
  *x=*y;
  *y=t;
}
int main(){
  int a,b;
  printf("请输入两个整数，用空格隔开；");
  scanf("%d %d",&a,&b);
  printf("交换之前的a,b值；%d %d\n",a,b);
  swap(&a,&b);
  printf("交换后两个整数的值为；%d %d\n",a,b);
  
  return 0;
}
